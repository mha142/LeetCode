class Solution:
    def isPalindrome(self, x: int) -> bool:
        x = str(x)
        y= ""
        for letter in reversed(x):
            y += letter

        if x == y:
            return True
        else: 
            return False 
        
